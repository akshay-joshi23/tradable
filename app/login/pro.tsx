import { LoginForm } from "../../components/LoginForm";

export default function ProLoginScreen() {
  return (
    <LoginForm
      role="pro"
      title="Pro sign in"
      subtitle="Sign in to start taking jobs."
    />
  );
}
